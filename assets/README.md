# Model assets

This directory contains locally downloaded, license-audited model assets used
for offline training and code-execution inference. Large files are ignored by
Git and must be pinned by repository revision and recorded with SHA-256 hashes.

Current approved asset:

- `BAAI/bge-small-en-v1.5` at revision
  `5c38ec7c405ec4b44b94cc5a9bb96e735b38267a` (MIT license)

Competition transcripts must never be uploaded to model hubs or external APIs.
